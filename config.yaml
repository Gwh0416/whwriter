# whwriter 配置文件
# 环境变量会覆盖此文件中的值

app:
  mode: dev          # dev | prod
  host: "0.0.0.0"
  port: 8080

mysql:
  host: "127.0.0.1"
  port: 3306
  user: whwriter
  password: whwriter123
  database: whwriter
  charset: utf8mb4

jwt:
  secret: "whwriter-jwt-secret-change-in-production"

smtp:
  host: ""            # 例如 smtp.qq.com
  port: 587
  user: ""            # 你的邮箱地址
  password: ""        # 邮箱授权码（非登录密码）
  from: ""            # 发件人地址，通常与 user 相同
